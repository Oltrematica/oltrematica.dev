{
  "name": "oltrematica-dev",
  "version": "1.0.0",
  "description": "Oltrematica Dev Team website powered by Jekyll and Tailwind CSS",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/styles.css --minify",
    "watch:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/styles.css --watch",
    "dev": "npm run watch:css & bundle exec jekyll serve --livereload",
    "build": "npm run build:css && bundle exec jekyll build",
    "serve": "bundle exec jekyll serve --livereload"
  },
  "keywords": [
    "jekyll",
    "tailwindcss",
    "blog",
    "static-site"
  ],
  "author": "Oltrematica Team",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "tailwindcss": "^3.4.0"
  }
}
